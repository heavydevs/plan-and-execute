interface:
  display_name: Plan and Execute
  short_description: Route direct, staged planning, and resumable execution economically
  default_prompt: 'Use $plan-and-execute. Apply DIRECT vs ORCHESTRATED first: keep cohesive small/medium work DIRECT and promote only substantial remaining outcomes. For ORCHESTRATED work, use FINAL_PLAN when the authoritative request is manageable; when request volume/breadth makes final planning credit-heavy or non-resumable, use isolated PRIMARY_PLAN to deterministically fragment/index the source and build a resumable prepared package first. Never load PRIMARY_PLANNING.md on normal FINAL_PLAN work. Route every leaf from its signals, never from the root model: never switch the root session model/effort; delegate leaves above the root tier to fresh workers at the floor tier. Escalate only from classified failure evidence. Preserve per-TODO provider/model tier/effort routing, deterministic validation, lifecycle recovery, and implementation changes. For evolving cross-TODO normative contracts, use versioned shared patterns with explicit signatories and reopen only stale completed signatories after a revision.'
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
