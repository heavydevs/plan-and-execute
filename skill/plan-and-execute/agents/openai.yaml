interface:
  display_name: Plan and Execute
  short_description: Orchestrate only long-horizon, resumable coding work
  default_prompt: 'Use $plan-and-execute. Apply its DIRECT vs ORCHESTRATED gate before creating any plan state. Prefer direct execution for cohesive small/medium work; use the full resumable TODO harness only when independent workstreams, broad research, migration risk, durable resume value, or isolation justify it. If direct work later grows, use the promotion handoff and plan only remaining outcomes. Preserve per-TODO provider/model tier/effort routing, deterministic validation, lifecycle recovery, and implementation changes.'
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
